{
  "name": "crm-eclat-net",
  "version": "1.0.0",
  "description": "Sistema de gestão de clientes moderno com React e Node.js",
  "main": "backend/dist/server.js",
  "scripts": {
    "build": "echo 'Building frontend...' && cd frontend && npm install && npm run build && echo 'Building backend...' && cd ../backend && npm install && npm run build && echo 'Build completed!'",
    "start": "cd backend && node dist/server.js",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": "18.x"
  }
}